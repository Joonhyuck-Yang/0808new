# Auth Service Railway 배포용 환경변수 (Gateway와 동일한 패턴)
PORT=8000
RAILWAY_ENVIRONMENT=true

# 로깅 설정
LOG_LEVEL=INFO
RAILWAY_LOG_JSON=true
RAILWAY_LOG_LEVEL=INFO

# 서비스 연결
GATEWAY_URL=https://gateway-production-be21.up.railway.app

# HTTP 클라이언트 설정
HTTP_TIMEOUT=30
HTTP_MAX_CONNECTIONS=100
HTTP_MAX_KEEPALIVE=20

# 추가 설정
RAILWAY_STATIC_URL=https://auth-service-production-aabc.up.railway.app
